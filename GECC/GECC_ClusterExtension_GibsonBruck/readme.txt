The code is currently setup to be run on a windows machine

Compile and run code by typing "make" in command line in this folder
or "make out=snm.exe target" if "make" does not work

You must have mingw installed for the compiler
http://mingw-w64.org/doku.php

Files run.sh, multiple_runs.sh, multiple_par.sh are for cluster usage
